{
	"name": "fpe-symphony",
	"version": "1.0.0",
	"description": "The symphony-api folder contains a modified version of the symphony-api node module. Once Symphony merges our changes, we can move to the node module instead of this.",
	"main": "index.js",
	"scripts": {
		"postinstall": "cd symphony-api&&npm install",
		"test": "echo \"Error: no test specified\" && exit 1",
		"start": "node index.js"
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"async": "^2.6.0",
		"body-parser": "^1.18.3",
		"express": "^4.16.3",
		"fs": "0.0.1-security",
		"path": "^0.12.7"
	}
}
